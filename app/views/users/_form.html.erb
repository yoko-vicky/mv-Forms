<%= form_with model: @user, local: true, class:"border border-light p-5 w-50 m-auto" do |f| %>
<% if @user.errors.any? %>
    <div id="error_explanation">
      <p><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</p>

      <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= f.label :username, "Your username" %>
<%= f.text_field :username, placeholder: "Username", class:"form-control mb-4" %>



<%= f.label :email, "Your email" %>
<%= f.text_field :email, placeholder: "Email", class:"form-control mb-4"%>



<%= f.label :password, "Your password" %>
<%= f.text_field :password, placeholder: "Password", class:"form-control mb-4"%>


<%= f.submit%>

<%end%>